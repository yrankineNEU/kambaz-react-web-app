import Labs from "./Labs";
import { HashRouter } from "react-router-dom";

export default function App() {
  return (
    <HashRouter>
      <div>
        <Labs />
      </div>
    </HashRouter>
  );
}
